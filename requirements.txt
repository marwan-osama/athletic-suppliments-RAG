pdfplumber>=0.11
langchain-text-splitters>=0.3
chromadb>=0.5
httpx>=0.27
streamlit>=1.41
# --- evaluation ---
# Only `python -m evaluation` and the Evaluate tab need these; the pipeline,
# the CLI and the rest of the UI import nothing from here.
ragas>=0.4,<0.5
langchain-openai>=0.3
datasets>=3.0
# ragas 0.4 imports langchain_community.chat_models.vertexai, which
# langchain-community removed in 0.4 — an unpinned install resolves to a
# combination that cannot be imported at all.
langchain-community>=0.3,<0.4

# --- source document ---
# Only `tools/build_source_pdf.py` needs this — it typesets the fact sheet into
# the PDF under `data/`. The pipeline reads that PDF and never imports reportlab.
reportlab>=4.0
